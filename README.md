# Simple Linear Regression 

The code is designated for teaching/demonstrating the simplest model of many machine learning models. The learner should implement the gradient derivation and update of the bias parameter B of the regression class.

Required packages:
``` numpy, python-mnist, and matplotlib ```
